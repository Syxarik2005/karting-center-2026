# День 3 — Тестирование · Картинг-центр «Апекс»

## Что внутри

| Что | Где |
| --- | --- |
| Тест-план (что покрыто, как запустить) | [`test-plan.md`](test-plan.md) |
| Баги (симптом → требования → причина → фикс → промпты → проверка → коммит) | [`bugs/`](bugs/) |
| Сами тесты | `02-development/client/src/**/*.test.ts(x)` |

## Найдено и исправлено 2 бага (при минимуме 1 из задания)

1. [BUG-001](bugs/BUG-001-mock-api-shared-references.md) — мок-API отдавал
   живые ссылки на состояние вместо копий, из-за чего «снимок до операции»
   молча превращался в «после» (поймано тестом на `createBooking`).
2. [BUG-002](bugs/BUG-002-vitest-config-breaks-tsc-build.md) — добавление
   Vitest сломало `npm run build` из-за несовпадения типов конфига.

## Как запустить тесты

```bash
cd 02-development/client
npm install
npm test        # vitest run — 18 тестов, все зелёные
npm run build   # прод-сборка — тоже проходит после BUG-002
```

## Как это соотносится с заданием летней школы

- **Тест-кейсы** — 18 реальных, запускаемых кейсов (не список на бумаге):
  юнит-тесты мок-API (контракт, бизнес-правила BR-01..BR-04) + компонентные
  тесты трёх ключевых экранов (бронирование, отмена, список броней).
- **1–3 найденных и исправленных бага** — 2, оба с полным циклом:
  симптом → требования → причина → фикс → промпты → ручная проверка → коммит.
